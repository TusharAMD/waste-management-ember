/* How to include file options in theme */
if( function_exists( 'hala_redux_setup' ) ) {
	hala_redux_setup( $file_option );
}

/* This is your option name where all the Redux data is stored. */
$opt_name = "hala_options";